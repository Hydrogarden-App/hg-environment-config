[Unit]
Description=Hydrogarden Docker Registry
After=docker.service
Requires=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a hg-registry
ExecStop=/usr/bin/docker stop -t 2 hg-registry

[Install]
WantedBy=multi-user.target
